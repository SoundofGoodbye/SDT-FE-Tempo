# API Configuration
NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_API_VERSION=

# Authentication
NEXT_PUBLIC_AUTH_DOMAIN=
EOF

# Create .env.local for delivery-tracker
cat > apps/delivery-tracker/.env.local << 'EOF'
# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:8081
NEXT_PUBLIC_API_VERSION=v1

# Authentication
NEXT_PUBLIC_AUTH_DOMAIN=localhost